import 'package:flutter/material.dart';

class AppColors {
  // ================= BRAND =================
  static const Color primary = Color(0xFF0F766E); // Teal
  static const Color secondary = Color(0xFF1E3A8A); // Indigo
  static const Color accent = Color(0xFF22D3EE); // Cyan
  static const Color accentAlt = Color(0xFF6366F1); // Indigo accent

  // ================= BACKGROUNDS =================
  static const Color background = Color(0xFFF8FAFC);
  static const Color darkBackground = Color(0xFF0F172A);
  static const Color surface = Colors.white;

  // ================= TEXT =================
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF334155);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textOnDark = Colors.white;
  static const Color chatBubbleBorder = Color(0xFFF1F5F9);

  // ================= UI ELEMENTS =================
  static const Color borderLight = Color(0xFFE2E8F0);
  static const Color shadow = Color(0x33000000); // 20% black
}
